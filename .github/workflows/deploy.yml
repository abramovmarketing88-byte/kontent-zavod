name: Deploy to debian-home

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, linux, debian-home]
    steps:
      - name: Pull main and rebuild on server
        run: sudo bash /opt/kontent-zavod/scripts/auto_update.sh
